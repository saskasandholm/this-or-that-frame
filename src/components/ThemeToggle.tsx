'use client';

// This is a placeholder component that does nothing
// The theme toggle functionality has been removed as we're using a single theme
// This file exists to prevent import errors from existing code that references it

export function ThemeToggle() {
  // Return null (renders nothing)
  return null;
}
